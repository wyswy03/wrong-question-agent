services:
  - type: web
    name: wrong-question-agent
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 8080
      - key: WRONG_QUESTION_BANK_DIR
        value: /data
